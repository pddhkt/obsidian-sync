 i would like to add features:
1. durable object as storage for collaborative mode, which should be a separate page from the current local index db page as there is already a existing data
2. as i would use the canvas to make a mock-up of UI, i would like to integrate the Claude into there for generating a mock via html. the approach can be that, after the user grouped some elements, and there should be a toolbar which already existed on the app, and we have a button about to generate the html for that element. we have to discuss about how we passing the element into the claude and let it generate the html, and how would it be rendered on the canvas. im thinking can we capture the element as a picture and let the claude to read the image and start to render it. discuss with me to see if there are any options. for the claude, i would prefer to use the claude agent sdk to handle it